name: CI
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-19.10
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2.1.2
        with:
          node-version: '12'
          check-latest: true
      - run: npm install
      - run: npm run build
